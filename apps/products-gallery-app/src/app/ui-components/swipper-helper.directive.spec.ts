import { SwipperHelperDirective } from './swipper-helper.directive';

describe('SwipperHelperDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipperHelperDirective();
    expect(directive).toBeTruthy();
  });
});
