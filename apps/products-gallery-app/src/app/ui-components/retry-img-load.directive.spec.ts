import { RetryImgLoadDirective } from './retry-img-load.directive';

describe('RetryImgLoadDirective', () => {
  it('should create an instance', () => {
    const directive = new RetryImgLoadDirective();
    expect(directive).toBeTruthy();
  });
});
