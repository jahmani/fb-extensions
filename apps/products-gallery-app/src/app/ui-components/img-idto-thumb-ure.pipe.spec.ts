import { ImgIdtoThumbUrePipe } from './img-idto-thumb-ure.pipe';

describe('ImgIdtoThumbUrePipe', () => {
  it('create an instance', () => {
    const pipe = new ImgIdtoThumbUrePipe();
    expect(pipe).toBeTruthy();
  });
});
