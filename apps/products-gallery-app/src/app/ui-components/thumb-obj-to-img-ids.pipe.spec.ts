import { ThumbObjToImgIdsPipe } from './thumb-obj-to-img-ids.pipe';

describe('ThumbObjToImgIdsPipe', () => {
  it('create an instance', () => {
    const pipe = new ThumbObjToImgIdsPipe();
    expect(pipe).toBeTruthy();
  });
});
