import { HydratedImgDirective } from './hydrated-img.directive';

describe('HydratedImgDirective', () => {
  it('should create an instance', () => {
    const directive = new HydratedImgDirective();
    expect(directive).toBeTruthy();
  });
});
