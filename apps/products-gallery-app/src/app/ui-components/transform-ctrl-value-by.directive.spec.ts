import { TransformCtrlValueByDirective } from './transform-ctrl-value-by.directive';

describe('TransformCtrlValueByDirective', () => {
  it('should create an instance', () => {
    const directive = new TransformCtrlValueByDirective();
    expect(directive).toBeTruthy();
  });
});
