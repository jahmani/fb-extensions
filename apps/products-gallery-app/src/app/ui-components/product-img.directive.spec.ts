import { ProductImgDirective } from './product-img.directive';

describe('ProductImgDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductImgDirective();
    expect(directive).toBeTruthy();
  });
});
